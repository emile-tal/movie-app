import { Text, View } from "react-native";

export default function Saved() {
  return (
    <View className="flex-1 min-w-full min-h-full items-center justify-center">
      <Text className="text-primary text-2xl font-bold">Welcome</Text>
    </View>
  );
}
